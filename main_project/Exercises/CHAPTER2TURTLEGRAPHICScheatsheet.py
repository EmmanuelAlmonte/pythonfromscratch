"""
cheat sheet for turtle.
T = turtle
T.forward # Moves the turtle forward 
T.right() # Rotates turtle a certain amount of degrees to the right. 
T.left() # Rotates the turtle a certain amount of degress to the left. 
T.setheading() #changes the angle of the turtle head.
T.dot() # Creates a dot in the path of the turtle 
T.pensize() # Changes the pen size
T.mainloop() # Creates a loop for the turtle/ This usually goes at the end. 
T.circle() # Creates a circle and the parameter is the radius
T.color() #
T.bgcolor() # change the background color
T.pencolor() # Change the pen color
T.goto() # move to specific location
T.reset(), T.clear(), T.clearscreen() # Each does soemthing similar but they are different. 
T.setup() # window width and height
T.pos() #displays the current turtle position. 
T.xcor() # displays the x coordinate.
T.ycor() # displays the y coordinate.
T.speed() # Range 0-10. 0 Being the fastest or no animation. if you don't include a number then you will get the current speed.  
T.hideturtle() # Hides the turtle 
T.write # This will display text in turtle. 
T.begin_fill() # start filling a shape. 
T.end_fill() # end filling a shape. 
T.fillcolor() # will change the color of the shape your filling. 
"""